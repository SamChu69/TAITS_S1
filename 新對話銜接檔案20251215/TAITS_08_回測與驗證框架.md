# 📘 TAITS_08_回測與驗證框架.md

（**第二批｜落地執行版：目錄結構 × I/O 契約 × 操作流程 × 報告模板**｜不寫程式也能照流程跑｜可直接存 GitHub）

---

## 0. 本批定位（為什麼一定要有這一批）

第一批你拿到的是 **「框架與原則」**。
但你真正需要的是這一層：

> **「就算換一個人、換一個 AI、換一個新對話，
> 只要照這個結構與流程，就不會跑偏、不會漏、不會亂解讀。」**

本批只做四件事，而且都做到「工程級」：

1. **回測專案目錄結構（與 TAITS_00～12 完全對齊）**
2. **每一層的輸入 / 輸出契約（I/O Contract）**
3. **完整回測執行流程（不要求你寫程式）**
4. **回測報告 Markdown 模板（可直接存檔）**

---

## 1. 世界一流內部評分標準（本批）

本批要達到 10/10，必須同時滿足：

1. **零通靈**：任何人看目錄就知道資料放哪
2. **零歧義**：每一層的輸入輸出都有明確欄位定義
3. **與 00～12 完全對齊**：不是額外一套系統
4. **不寫程式也能跑流程**：流程文件即操作手冊
5. **回測結果可審計**：知道為什麼好、為什麼爛
6. **支援分批跑**：資料再大也不會爆
7. **支援中小型股題材輪動**
8. **治理/風控可被驗證是否過嚴**
9. **回測輸出能直接餵給 Paper / Live**
10. **新對話 100% 看得懂**

---

## 2. TAITS 回測專案目錄結構（正式版）

> 📁 **taits_backtest/**
> （此結構可直接建立於 GitHub，與 docs/ 並行）

```
taits_backtest/
│
├─ config/
│   ├─ backtest_profile.yaml        # 回測設定（期間/市場/模式）
│   ├─ regime_config.yaml            # Regime 判定參數
│   ├─ risk_config.yaml              # 風控門檻（對齊 TAITS_07）
│   └─ cost_model.yaml               # 成本/滑價假設
│
├─ data/
│   ├─ raw/                           # 原始資料（不可修改）
│   ├─ processed/                    # 清洗後資料
│   ├─ features/                     # TAITS_03 輸出
│   └─ health_logs/                  # 資料健康紀錄
│
├─ regimes/
│   ├─ regime_series.parquet         # 每日/每 bar Regime
│   └─ regime_audit.md               # Regime 判定說明
│
├─ strategies/
│   ├─ signals/                      # TAITS_05 原始策略輸出
│   ├─ aggregated/                   # 多策略彙總
│   └─ strategy_cards/               # 策略卡（Markdown）
│
├─ governance/
│   ├─ permission_logs/              # L0~L4 裁決紀錄
│   └─ weight_logs/                  # 權重計算過程
│
├─ risk/
│   ├─ risk_events/                  # R0~R3 事件紀錄
│   └─ kill_switch_logs/             # 停機紀錄
│
├─ execution/
│   ├─ order_intents/                # 模擬下單意圖
│   ├─ fills/                        # 模擬成交
│   └─ slippage_reports/             # 滑價分析
│
├─ reports/
│   ├─ system_report.md              # 系統級報告
│   ├─ regime_reports/               # 各 Regime 報告
│   └─ strategy_rankings.md          # 策略排行榜
│
└─ audit/
    ├─ version_lock.json             # 版本鎖
    └─ run_manifest.md               # 本次回測總清單
```

---

## 3. 各層輸入 / 輸出契約（I/O Contract）

### 3.1 Data → Feature（TAITS_02 → TAITS_03）

**輸入（Data）**

* OHLCV
* 成交額
* 漲跌停旗標
* 期貨/選擇權/融資融券/事件觀察欄位
* `data_health_flag`

**輸出（Features）**

* 技術指標（MA/GMMA/CBL…）
* 結構特徵（威科夫、纏論、鮑迪克）
* 動能/波動/成交結構
* 每個特徵必帶 `confidence`

---

### 3.2 Feature → Strategy（TAITS_03 → TAITS_05）

**輸入**

* Feature vectors
* Regime（僅作條件，不作預測）

**輸出（每條策略）**

* `strategy_id`
* `signal`（多/空/中立）
* `score`
* `confidence`
* `intended_regime`
* `explanation_zh`

---

### 3.3 Strategy → Governance（TAITS_05 → TAITS_06）

**輸入**

* 多策略 signals
* Regime state
* 觀察層（FU/OP/MG/EV）

**輸出**

* `allowed_level`（L0~L4）
* `position_cap`
* `cooldown`
* `reason_zh`

---

### 3.4 Governance → Risk（TAITS_06 → TAITS_07）

**輸入**

* Intent（可能的）
* 組合狀態
* 市場風險狀態

**輸出**

* `risk_level`（R0~R3）
* `risk_constraints[]`
* `kill_switch`（true/false）

---

### 3.5 Risk → Execution Simulation

**輸入**

* 通過風控的 order_intent
* 成本模型
* 市場流動性代理

**輸出**

* 模擬成交（fills）
* 滑價
* 未成交原因

---

## 4. 回測執行流程（Step-by-Step，不寫程式版）

### Step 1｜鎖定版本

* 填寫 `audit/version_lock.json`
* 指定 TAITS_03/04/05/06/07 版本

### Step 2｜準備資料

* 原始資料放 `data/raw/`
* 不足資料標記 `health_flag`

### Step 3｜產生 Features

* 輸出至 `data/features/`

### Step 4｜跑 Regime

* 產出 `regimes/regime_series`

### Step 5｜跑策略

* 334 策略全跑
* 輸出 `strategies/signals/`

### Step 6｜治理裁決

* 產生 L0~L4 裁決紀錄

### Step 7｜風控裁決

* 套用 TAITS_07
* 產生 R0~R3 事件

### Step 8｜執行模擬

* 撮合、滑價、部分成交

### Step 9｜產出報告

* System / Regime / Strategy 三層報告

---

## 5. 回測報告 Markdown 模板（可直接用）

### 5.1 System Report（system_report.md）

```md
# TAITS 回測系統報告

## 回測摘要
- 期間：
- 市場：
- 策略數：
- Regime 分布：

## 整體績效
- CAGR：
- MDD：
- 勝率：
- 成本占比：

## 治理與風控統計
- L0~L4 分布：
- R0~R3 事件次數：

## 中小型股通道分析
- 命中題材：
- 爆發捕捉率：

## 結論與限制
```

---

### 5.2 Strategy Card（strategy_cards/*.md）

```md
# Strategy Card｜<strategy_id>

## 定位
- 家族：
- 適用 Regime：

## 績效（分 Regime）
| Regime | CAGR | MDD | 勝率 |

## 成本敏感度
- 高 / 中 / 低

## 風控觸發
- 主要風控規則：

## 中文說明
```

---

## 6. 回測輸出如何接到 Paper / Live

* `order_intent` → 直接給 TAITS_09（實戰流程）
* `strategy_cards` → 策略上線/下線依據
* `risk_events` → 風控參數微調依據
* `regime_reports` → Regime 引擎校正

---

## 7. 本批交付聲明（08 完整完成）

* ✔ 回測專案目錄結構（工程級）
* ✔ 各層 I/O 契約（零歧義）
* ✔ 不寫程式的完整操作流程
* ✔ Markdown 報告模板
* ✔ 與 TAITS_00～12 完全對齊

**TAITS_08 至此完整鎖定。**
