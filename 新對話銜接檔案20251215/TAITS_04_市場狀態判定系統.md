# 📘 TAITS_04_市場狀態判定系統.md

（Regime Engine 完整落地規格｜台股/期貨/選擇權/融資融券/外資籌碼/事件與情緒全納入｜可否決一切策略｜可回測可實盤｜可直接存 GitHub）

---

## 0. 文件定位（非常重要）

本文件定義 **TAITS 的「市場狀態判定系統（Market Regime Engine）」**，其目的不是產生交易訊號，而是：

* 把市場分成可被治理與風控引用的「狀態（Regime）」
* 提供策略層的 **Filter / Weight Adjuster / Risk Override / Execution Constraint**
* 支援「只觀察」模式（你要求：不一定下單，先看懂市場）
* **Risk / Compliance 可否決一切**（最高原則）

嚴格定位：

* ❌ Regime ≠ 策略
* ❌ Regime 不直接下單
* ✅ Regime 是 TAITS 的「決策場景底座」
* ✅ Regime 結果會影響：策略是否啟用、權重如何調整、是否降級、是否進入人工覆核

---

## 1. 世界一流內部評分標準（10/10 必要條件）

1. **全市場覆蓋**：價格/成交量/波動/趨勢/流動性/籌碼/期貨/選擇權/融資融券/事件/情緒/宏觀
2. **可落地**：定義輸入→特徵→狀態→輸出介面→治理掛鉤
3. **可回測可實盤一致**：03P/03Q/03R/03S 可 1:1 對齊
4. **可否決**：Regime Engine 可在需要時否決全部策略或限制行為
5. **可解釋**：每個狀態都有中文解釋、觸發條件、對策略影響
6. **可擴充**：新因子/新資料源能插拔，不破壞舊規格
7. **抗噪**：防止單一訊號誤判，使用多證據與置信度
8. **具備韌性**：缺資料、延遲、異常都有降級策略
9. **具備審計**：輸出包含證據鏈與版本標籤
10. **可治理**：與 Strategy Permission Gate/03O/風控降級強耦合

---

## 2. Regime Engine 在 TAITS 的位置與輸入輸出

### 2.1 主要輸入（Inputs）

Regime Engine 以「市場級 + 族群級 + 個股級」三層輸入：

* **市場級（Market-Level）**：加權指數、櫃買指數、台指期、VIX/波動代理、資金流、事件衝擊
* **族群級（Sector/Theme-Level）**：電子/金融/傳產、題材（AI/PCB/DRAM/機器人…）輪動強弱
* **個股級（Symbol-Level）**：個股趨勢、波動、流動性、籌碼壓力（不等於策略訊號）

### 2.2 主要輸出（Outputs）

Regime Engine 輸出一個標準化物件 `RegimeState`：

* `regime_primary`：主狀態（例：趨勢多頭、趨勢空頭、盤整、崩盤/恐慌、事件衝擊…）
* `regime_secondary`：次狀態（例：高波動、低流動性、結算壓力、避險盤…）
* `confidence`：置信度（0~1）
* `evidence_bundle`：證據包（哪些特徵支持）
* `action_constraints`：行為限制（禁止加碼/只允許小單/只觀察）
* `weight_adjustments`：權重調整（策略/族群/風險預算）
* `override_flags`：覆寫旗標（risk_off、manual_review_required…）
* `version_tags`：版本與資料快照（03R/03S 對齊）

---

## 3. Regime 分層與狀態集合（Regime Taxonomy）

> 這裡是「不可以少」的重點：狀態要定義清楚，新對話才知道你在講什麼。

### 3.1 主狀態（Primary Regime）— 8 大類

1. **R1 趨勢多頭（Trend Bull）**
2. **R2 趨勢空頭（Trend Bear）**
3. **R3 盤整震盪（Range / Chop）**
4. **R4 高波動趨勢（High-Vol Trend）**
5. **R5 恐慌崩跌（Panic / Crash）**
6. **R6 事件衝擊（Event Shock）**
7. **R7 結算/籌碼壓力（Settlement & Positioning Pressure）**
8. **R8 低流動性/失真市場（Liquidity Distortion）**

### 3.2 次狀態（Secondary Regime）— 可疊加（多選）

* S1 高波動（High Volatility）
* S2 低波動（Low Volatility）
* S3 低流動性（Low Liquidity）
* S4 籌碼偏多（Positioning Long）
* S5 籌碼偏空（Positioning Short/Hedge）
* S6 避險模式（Risk-Off / Hedge On）
* S7 題材輪動加速（Fast Rotation）
* S8 結算壓力（Expiration/Settlement Pressure）
* S9 假突破/假跌破風險（Fake Move Risk）
* S10 系統性風險升高（Systemic Risk Rising）

---

## 4. Regime 特徵群（Feature Groups）與資料來源類型

> 你要求「期貨/選擇權/融資融券都是觀察用」，本節把它們納入 Regime 的正確位置：**Filter / Risk / Weight**。

### G1：價格與趨勢（Price/Trend）

* MA/EMA 趨勢斜率、結構高低點、突破/跌破、趨勢持續度

### G2：成交量與量能結構（Volume/Participation）

* 放量/縮量、量價背離、成交量集中度、換手率

### G3：波動與風險（Volatility/Risk）

* ATR%、日內波幅、跳空率、尾部風險代理

### G4：流動性（Liquidity）

* 成交額、bid/ask（若有）、滑價代理、可成交容量估計

### G5：市場廣度（Breadth）

* 上漲家數/下跌家數、創高創低比、族群擴散/收斂

### G6：籌碼與資金流（Positioning/Flow）

* 外資/投信/自營、主力籌碼代理、資金集中度

### G7：期貨影響（Futures Impact）【觀察用】

* 台指期方向、OI 變化、期現價差（Basis）、領先轉折、避險行為

### G8：選擇權壓力（Options Pressure）【觀察用】

* Call/Put OI 牆、最大痛點、Gamma 壓力、結算鎖價

### G9：融資融券（Margin/Short）【觀察用】

* 融資餘額變化、融券餘額變化、券資比、追繳風險代理

### G10：事件與情緒（Event/Sentiment）【觀察用】

* 重大新聞事件、政策/法規、地緣政治、社群情緒、突發性黑天鵝

---

## 5. Regime 判定流程（從資料到狀態）

### 5.1 流程總覽（必備）

1. **資料匯入**：02（資料來源）→ Data Ingestion
2. **特徵計算**：03（指標/特徵）→ Feature Pipeline
3. **子引擎評分**：各 Feature Group 產生 `score`（-1~+1）
4. **Regime 融合**：多證據融合得到主狀態 + 次狀態
5. **置信度計算**：一致性、資料品質、新鮮度
6. **治理掛鉤**：產生約束（constraints）與權重調整（weights）
7. **輸出審計**：寫入 evidence_bundle 與版本標籤
8. **廣播給全系統**：供策略層/風控/03O/告警使用

---

## 6. Regime 融合器（Fusion Logic）— 多證據而非單一訊號

### 6.1 子分數（Sub-score）標準

每個子引擎輸出：

* `direction_score`：方向（-1~+1）
* `risk_score`：風險（0~1）
* `liquidity_score`：流動性（0~1）
* `structure_score`：結構有效性（0~1）
* `confidence`：置信度（0~1）
* `evidence`：證據（指標值、事件、變化率、異常點）

### 6.2 融合規則（示意，非唯一）

* 趨勢方向以 G1/G2/G5 為主
* 風險與否決以 G3/G4/G10 為主
* 結算與籌碼壓力以 G7/G8/G9 為主
* **任何時候 Risk/Compliance 可強制 risk_off**

---

## 7. 每個 Regime 的「策略影響矩陣」（最重要）

> 你說：中小型股爆發力不能被嚴苛篩掉。
> 因此 Regime 不做「只剩龍頭股」的決定，而是做「行為限制與權重調整」，把機會留給你策略層去挑。

### R1 趨勢多頭（Trend Bull）

* 允許：趨勢型/突破型/題材輪動型
* 限制：追高需量能確認；高波動則降級
* 權重：中小型股可加題材權重，但需流動性下限

### R2 趨勢空頭（Trend Bear）

* 允許：只觀察、或防守型（若你允許）
* 限制：新倉嚴格、倉位上限下降、停利更快
* 覆寫：可能強制 `risk_off`

### R3 盤整震盪（Range/Chop）

* 允許：短線/均值回歸/區間
* 限制：趨勢策略降權，避免被洗
* 權重：降低槓桿、提高出場優先

### R4 高波動趨勢（High-Vol Trend）

* 允許：趨勢可做但要縮倉
* 限制：分批、停止加碼、提高 03O 成本門檻
* 告警：S2 風險告警

### R5 恐慌崩跌（Panic/Crash）

* 允許：只觀察（預設）
* 限制：禁止新倉、取消掛單、強制降級
* 覆寫：`stop_all` 或 `degrade_to_observe`

### R6 事件衝擊（Event Shock）

* 允許：事件後確認再做
* 限制：進人工覆核；風險門檻上調
* 覆寫：`manual_review_required`

### R7 結算/籌碼壓力（Settlement/Positioning）

* 允許：短線，但避開被「鎖價/擠壓」
* 限制：選擇權牆附近不追；期現價差異常降權
* 權重：對特定族群/權值做偏置調整（非下單）

### R8 低流動性/失真（Liquidity Distortion）

* 允許：只做高流動性或只觀察
* 限制：中小型股需要更高流動性下限（但不消滅）
* 覆寫：`allow_small_only` 或 `observe_only`

---

## 8. Regime 與 03O（送單前檢查）的硬對齊

Regime Engine 輸出必被 03O 使用，至少包含：

* `risk_off_flag`
* `liquidity_stress_flag`
* `high_volatility_flag`
* `event_shock_flag`
* `settlement_pressure_flag`
* `recommended_max_size_multiplier`
* `recommended_order_type_list`
* `manual_review_required_flag`

> 你要的「能不能下單由你決定」：
> 系統可以建議與限制，但 **是否啟用自動化送單仍由你決定**。

---

## 9. Regime 的審計輸出格式（新對話必讀得懂）

每次狀態更新寫入：

* `regime_primary`（中文+英文代碼）
* `regime_secondary[]`
* `confidence`
* `evidence_bundle`（至少 5 條關鍵證據）
* `constraints`（限制）
* `weight_adjustments`（調整）
* `override_flags`
* `version_tags`（release_id / config_revision / ruleset_lock / data_snapshot_hash）
* `timestamp`

---

## 10. Regime Engine 的落地配置接口（對齊 03S）

> 你已建立 03S YAML 模板，Regime Engine 需有專屬區塊（下一批我會補在 03S 中的 `config_base/??` 或新增 `config_base/13_Regime配置.yaml`，但你現在要 04 文件先完整落地，所以先在此定義接口。）

* `taits.regime_engine.enabled`
* `taits.regime_engine.taxonomy_version`
* `taits.regime_engine.feature_group_weights`
* `taits.regime_engine.hard_stop_rules`
* `taits.regime_engine.output_broadcast_interval_sec`
* `taits.regime_engine.audit.enabled`

---

## 11. 完整性鎖定聲明（04 文件）

* ✔ 涵蓋：價格、量能、波動、流動性、廣度、籌碼、期貨、選擇權、融資融券、事件情緒
* ✔ 定義：主狀態 8 類 + 次狀態 10 類（可疊加）
* ✔ 輸出：可否決、可降級、可調權、可審計、可回放
* ✔ 對齊：03P/03Q/03R/03S（RunID 四件套）
* ✔ 不新增策略、不承諾績效
* ✔ 新對話讀此文件可 100% 知道 TAITS 的 Regime 怎麼做
