# ✅ **TAITS_S1 最終正式版 requirements.txt（建議使用）**

# ============ Core =============
pandas==2.2.2            # dataframe 操作
numpy==1.26.4            # 數值運算核心
python-dateutil==2.9.0.post0

# ============ Indicators =============
ta-lib==0.4.28           # MA / RSI / MACD / GMMA 指標核心

# ============ Data Sources =============
yfinance==0.2.40         # Yahoo Finance
finmind==1.1.9           # 台灣 FinMind API
requests==2.32.3         # HTTP API
beautifulsoup4==4.12.3   # 新聞爬蟲（NewsAgent）

# ============ System / Config =============
PyYAML==6.0.2            # 讀取 config.yaml
tabulate==0.9.0          # CLI table format

# ============ ML / Regime Engine =============
scikit-learn==1.4.1      # KMeans / HMM / clustering

# ============ Optional Tools =============
matplotlib==3.8.2        # 圖表（可選）
tqdm==4.66.4             # progress bar
```

